pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                echo 'Cloning source code from Git...'
                checkout scm
            }
        }
    }
}

